

## Переделка блока «Услуги» на странице О компании

### Что меняем

Компонент `AboutServices.tsx`:

1. **Убираем инверсию при наведении** — вместо смены фона на `#BA846E` при hover, добавляем только тень (`hover:shadow-lg`)
2. **Первая строка (основные услуги)** — карточки с `is_featured = true` рендерятся первыми, с фоном `#BA846E` и белым текстом/иконками (всегда, не по hover)
3. **Остальные карточки** — белый фон, чёрный текст, терракотовая иконка, при наведении только тень
4. **Убираем курсив** — `italic` убираем из описания
5. **Иконки остаются** — в правом верхнем углу карточки
6. **Убираем скрытую надпись «Подробнее →»** — она больше не нужна без инверсии
7. **CTA «Заинтересовали?»** — остаётся последней карточкой

### Шаги

1. **`src/components/about/AboutServices.tsx`** — переписать рендер:
   - Разделить `publishedServices` на `featured` (`is_featured=true`) и `regular`
   - Featured карточки: `bg-[#BA846E] text-white`, иконка белая, тень при hover
   - Regular карточки: `bg-white`, текст чёрный, иконка `text-[#BA846E]`, `hover:shadow-lg`
   - Описание без `italic`
   - Убрать span «Подробнее →»

